[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = --tb=short -p no:warnings
markers =
    slow: tests that require model weights (skip with -m "not slow")
    gpu: tests that require CUDA/XPU (skip with -m "not gpu")
